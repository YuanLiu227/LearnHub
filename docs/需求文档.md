# LearnHub 学习内容搜索平台 — 需求文档

## 1. 项目概述

### 1.1 项目定位
LearnHub 是一个关键词驱动的多源学习资源搜索平台，支持博主内容自动收集，帮助开发者从多个技术平台快速检索和发现高质量学习内容。

### 1.2 目标用户
- 想要系统性学习某一技术领域的开发者
- 需要跨平台追踪特定博主/频道最新内容的用户
- 希望收藏和管理学习资源的用户

### 1.3 核心价值
- **多源聚合**：一次搜索覆盖 Bilibili、YouTube、编程导航、鱼皮AI导航
- **博主追踪**：关注 UP 主/频道，自动收集最新内容（仅按时间筛选）
- **资源管理**：收藏、完成标记、归档/恢复、选择式批量删除、一键清空
- **数据隔离**：多用户系统，每个用户独立管理自己的数据

---

## 2. 用户系统

### 2.1 注册流程（两步验证）

```
Step 1: 提交邮箱 + 密码 + 图形验证码
                ↓
        发送 6 位数字邮箱验证码
        开发模式（SMTP 未配置）直接返回 devCode
                ↓
Step 2: 提交邮箱 + 邮箱验证码
                ↓
        校验通过 → 创建用户 → 自动登录（返回 JWT）
```

| 功能 | 描述 |
|------|------|
| 邮箱注册 | 使用邮箱作为账号，注册时发送验证码到邮箱验证 |
| 密码安全 | 密码需包含字母、数字、特殊字符中的至少两种，长度至少 6 位 |
| 图形验证码 | 4 位数字 SVG 验证码，防止机器人批量注册，一次性使用，5 分钟过期 |
| 邮箱验证码 | 6 位数字，10 分钟有效期，存储于数据库 verification_codes 表 |
| JWT 认证 | 登录后签发 JWT，7 天过期，存储于 localStorage |

### 2.2 登录流程

| 功能 | 描述 |
|------|------|
| 邮箱+密码登录 | 需输入图形验证码，bcrypt 校验密码 |
| JWT 持久化 | Token 存储在 localStorage，每次请求通过 Authorization header 携带 |
| 自动登录恢复 | 页面刷新时读取 localStorage 的 token，调用 GET /auth/me 验证有效性 |
| 401 自动跳转 | 所有 API 响应 401 时自动清除 token 并跳转登录页 |

### 2.3 图形验证码系统

- 服务端内存存储（Map），key 为 captchaId
- 4 位数字，随机颜色、旋转、干扰线、干扰点
- SVG 格式输出，前端通过 `data:image/svg+xml;base64` 渲染
- **一次性使用**：验证成功后立即删除
- **自动清理**：每分钟扫描一次，清除过期（5 分钟）的验证码
- 登录、注册第一步、重新发送验证码均需图形验证码
- 注册第二步（邮箱验证）不需要图形验证码

### 2.4 邮箱验证码系统

- 存储于 `verification_codes` 表，记录邮箱、验证码、类型、过期时间、是否使用
- 10 分钟有效期，验证成功后标记 `used = 1`
- 通过 nodemailer + SMTP 发送邮件
- SMTP 未配置时自动返回 `devCode`，方便开发调试

---

## 3. 功能需求

### 3.1 关键词管理

| 功能 | 描述 |
|------|------|
| 添加关键词 | 用户输入关键词，系统存储并用于后续搜索 |
| 启用/暂停 | 切换关键词的监控状态，暂停的关键词不会被搜索 |
| 归档关键词 | 关键词归档后不再搜索，但已匹配的学习资源保留 |
| 恢复关键词 | 将已归档的关键词重新激活 |
| 永久删除 | 删除关键词及其关联的所有学习资源 |
| 关键词总览 | 展示所有关键词（含已归档），查看每个关键词的资源数，支持恢复 |

### 3.2 博主关注管理

| 功能 | 描述 |
|------|------|
| 添加关注 | 输入 UP 主名称（Bilibili）或频道名称（YouTube），自动搜索并关注 |
| Bilibili 关注 | 通过 Bilibili 搜索 API 查找 UP 主，支持空间 API 和搜索 API 回退 |
| YouTube 关注 | 通过 YouTube Search API 查找频道 |
| 启用/暂停 | 切换博主的收集状态 |
| 归档博主 | 不再收集该博主内容，已收集的资源保留 |
| 恢复博主 | 将已归档的博主重新激活 |
| 永久删除 | 删除博主及其关联的所有资源（排除收藏资源） |
| 博主主页 | 点击外部链接图标跳转到博主主页 |
| 博主总览 | 展示所有博主（含已归档），支持恢复 |

### 3.3 博主内容收集

| 功能 | 描述 |
|------|------|
| 触发收集 | 用户手动触发，系统从所有已启用的博主收集最新视频 |
| 独立于关键词 | 博主收集不经过关键词搜索流程，不使用热度/质量阈值 |
| 时间筛选 | 仅按发布时间收集近期内容（默认近 7 天） |
| 进度反馈 | 实时展示收集进度（轮询 `/api/creators/collect/progress/:id`） |
| 自动定时任务 | 每天早上 08:00 自动执行全量博主内容收集（node-cron） |
| Bilibili 回退 | 空间 API 限流时自动回退到搜索 API，按发布排序筛选该 UP 主视频 |

### 3.4 学习资源搜索（关键词）

| 功能 | 描述 |
|------|------|
| 触发搜索 | 用户手动触发，在所有启用的关键词下跨源搜索 |
| 质量阈值 | 关键词搜索的视频需满足最低播放/点赞/收藏阈值 |
| 进度反馈 | 实时展示搜索的阶段（轮询 `/api/monitor/progress/:id`） |
| 结果存储 | 搜索结果持久化到 SQLite 数据库 |
| 综合评分 | 每条资源按公式计算综合分并排序展示 |
| 去重机制 | 跨来源内容去重，采用 Bigram Jaccard 相似度算法 |
| 通知面板 | 搜索完成后弹出通知，展示新增资源条目 |

### 3.5 资源管理

| 功能 | 描述 |
|------|------|
| 资源列表 | 分页展示资源，按热度倒序排列 |
| 收藏资源 | 点击星标收藏，收藏后移出资源总览，仅在收藏标签页显示 |
| 取消收藏 | 取消收藏后检测关联实体是否存在：实体存在则回到总览，已删除则级联删除 |
| 完成标记 | 标记学习完成，视觉反馈（完成样式） |
| 单条删除 | 含确认对话框防止误操作 |
| 选择式批量删除 | 进入选择模式勾选多个资源，统一删除 |
| 一键清空 | 清空当前分类（关键词/博主）所有资源，含确认弹窗 |
| 知识搜索 | 全文搜索已有资源（同时搜索 news_items 和 hot_topics 两张表，去重 URL） |

### 3.6 数据统计

| 功能 | 描述 |
|------|------|
| 统计概览 | 展示总资源数、今日新增、启用关键词数 |
| 资源热力 | 资源按热度（heat）倒序排列 |

### 3.7 配置管理

| 功能 | 描述 |
|------|------|
| 邮箱配置 | 设置通知接收邮箱 |

### 3.8 AI 服务

| 功能 | 描述 |
|------|------|
| AI 对话 | POST `/api/ai/chat`，基于 DeepSeek V4 Flash |
| 内容真实性校验 | POST `/api/ai/veracity`，校验内容真实性并返回置信度 |

---

## 4. 数据源

| 数据源 | 接口方式 | 鉴权 | 用途 |
|--------|----------|------|------|
| Bilibili | 搜索 API + 空间 API | 无需密钥 | 关键词搜索 + 博主关注 |
| YouTube | Data API v3 (Search + Video Stats) | API Key | 关键词搜索 + 博主关注 |
| 编程导航 (codefather.cn) | 文章列表抓取 | 无需密钥 | 关键词搜索 |
| 鱼皮AI导航 (ai.codefather.cn) | 文章列表抓取 | 无需密钥 | 关键词搜索 |

---

## 5. 非功能需求

### 5.1 技术栈
| 层级 | 技术 |
|------|------|
| **前端** | React 19 + Vite 6 + TypeScript 5 + Tailwind CSS 4 |
| **状态管理** | Zustand 5 |
| **动画** | motion/react |
| **图标** | lucide-react |
| **后端** | Node.js + Express 5 + TypeScript（tsx 运行时） |
| **数据库** | SQLite（better-sqlite3，WAL 模式） |
| **认证** | bcryptjs + jsonwebtoken（JWT，7 天过期） |
| **AI 服务** | DeepSeek V4 Flash API（内容摘要、质量评估、真实性校验） |
| **邮件** | nodemailer（SMTP） |
| **定时任务** | node-cron |
| **HTTP 客户端** | axios（前后端通用） |
| **包管理** | npm workspaces（client/ + server/） |

### 5.2 数据库需求
- SQLite 数据库，WAL 模式提高并发读写性能
- 数据库文件位置可通过 `DB_PATH` 环境变量自定义
- 支持渐进式迁移（ALTER TABLE ADD COLUMN）

**数据库表结构**：

| 表名 | 说明 |
|------|------|
| `users` | 用户表（id, email, password_hash, created_at） |
| `verification_codes` | 邮箱验证码表（id, email, code, type, expires_at, used） |
| `keywords` | 关键词表（id, term, enabled, archived, user_id） |
| `followed_creators` | 关注博主表（id, platform, channel_id, channel_name, enabled, archived, user_id） |
| `news_items` | 学习资源表（id, keyword_id, title, url, source, heat, creator_id, completed, favorited, user_id） |
| `hot_topics` | 热点话题表（id, title, url, source, heat） |
| `config` | 配置表（key-value 结构，支持 user_id） |

### 5.3 性能
- 搜索请求异步执行，前端通过 500ms 间隔轮询进度
- 数据库 WAL 模式提高并发读写
- 博主收集和关键词搜索独立执行，互不阻塞
- ProgressEmitter 在任务完成 5 分钟后自动清理，防止内存泄漏

### 5.4 安全
- 密码通过 bcrypt 哈希存储（10 轮 salt）
- JWT 认证中间件保护所有需要登录的 API
- 用户数据隔离：所有查询均包含 `AND user_id = ?` 条件
- API 密钥通过环境变量注入，不硬编码
- 图形验证码防止机器人批量注册
- 前端通过 Vite proxy 转发 API 请求，避免跨域问题

---

## 6. 综合评分公式（关键词搜索）

```
综合分 = 热度(heat) × 0.4
       + 来源可信度(credibility) × 0.3
       + 时效性(timeliness) × 0.2
       + 内容类型(contentType) × 0.1
```

| 维度 | 权重 | 取值规则 |
|------|------|----------|
| 热度 | 0.4 | `min(100, 播放量/1000)`，归一化到 0~1 |
| 来源可信度 | 0.3 | 编程导航/鱼皮AI导航=0.9, Bilibili=0.6, YouTube=0.8 |
| 时效性 | 0.2 | 7 天内=1.0, 30 天内=0.7, 90 天内=0.4, 更早=0.2 |
| 内容类型 | 0.1 | AI 课程=0.9, 教程文章=0.8, 视频=0.7 |

> 注：博主内容收集不经过评分系统，仅按发布时间排序。

### 去重机制
跨来源内容去重采用 Bigram Jaccard 相似度算法：
- **标题相似度阈值**：0.75
- **来源相似度阈值**：0.6
- 检测到重复时，保留来源可信度更高的条目

### 来源信用评级

| 来源 | 可信度 | 说明 |
|------|--------|------|
| 编程导航 | 0.9 | 人工筛选的技术文章，质量最高 |
| 鱼皮AI导航 | 0.9 | 人工筛选的 AI 课程，质量最高 |
| YouTube | 0.7 | 视频平台，质量参差不齐 |
| Bilibili | 0.6 | 视频平台，质量参差不齐 |

---

## 7. API 接口一览

### 7.1 认证
| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/api/auth/captcha` | 获取图形验证码（SVG） | ❌ |
| POST | `/api/auth/register` | 注册第一步：提交邮箱+密码+图形验证码，发送邮箱验证码 | ❌ |
| POST | `/api/auth/register/verify` | 注册第二步：提交邮箱验证码，完成注册 | ❌ |
| POST | `/api/auth/login` | 邮箱+密码+图形验证码登录 | ❌ |
| POST | `/api/auth/resend-code` | 重新发送邮箱验证码 | ❌ |
| GET | `/api/auth/me` | 获取当前用户信息 | ✅ |

### 7.2 关键词
| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/api/keywords` | 获取活跃关键词列表 | ✅ |
| GET | `/api/keywords/all` | 获取全部关键词（含已归档） | ✅ |
| POST | `/api/keywords` | 添加关键词 | ✅ |
| POST | `/api/keywords/archive` | 归档关键词（软删除） | ✅ |
| POST | `/api/keywords/restore` | 恢复已归档关键词 | ✅ |
| DELETE | `/api/keywords/permanent` | 永久删除关键词（含关联资源） | ✅ |
| PATCH | `/api/keywords` | 更新关键词（启用/暂停） | ✅ |

### 7.3 博主关注
| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/api/creators` | 获取活跃博主列表 | ✅ |
| GET | `/api/creators/all` | 获取全部博主（含已归档） | ✅ |
| POST | `/api/creators` | 添加博主（按名称/频道搜索） | ✅ |
| PATCH | `/api/creators/:id` | 更新博主（启用/暂停） | ✅ |
| DELETE | `/api/creators/:id` | 归档博主（软删除） | ✅ |
| POST | `/api/creators/restore/:id` | 恢复已归档博主 | ✅ |
| DELETE | `/api/creators/permanent/:id` | 永久删除博主（含关联资源） | ✅ |
| POST | `/api/creators/collect` | 触发博主内容收集（异步） | ✅ |
| GET | `/api/creators/collect/progress/:id` | 查询收集进度 | ❌ |

### 7.4 搜索监控
| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/monitor` | 触发全量关键词搜索（异步） | ✅ |
| GET | `/api/monitor/progress/:id` | 查询搜索进度 | ❌ |
| GET | `/api/monitor/history` | 获取历史搜索结果（可选按关键词筛选） | ✅ |

### 7.5 数据统计与资源管理
| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/api/dashboard/stats` | 获取统计概览 | ✅ |
| GET | `/api/dashboard/hotspots` | 获取资源列表（分页，可选按来源筛选） | ✅ |
| GET | `/api/dashboard/search` | 全文搜索已存储资源（跨 news_items 和 hot_topics） | ✅ |
| GET | `/api/dashboard/favorites` | 获取收藏资源列表（分页） | ✅ |
| PATCH | `/api/dashboard/resource/:id` | 更新资源（收藏/取消收藏/标记完成） | ✅ |
| DELETE | `/api/dashboard/resource/:id` | 删除单个资源 | ✅ |
| POST | `/api/dashboard/resources/batch-delete` | 按类型批量清空（关键词/博主） | ✅ |
| POST | `/api/dashboard/resources/batch-delete-by-ids` | 按 ID 批量删除 | ✅ |

### 7.6 AI
| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/ai/chat` | AI 对话 | ❌ |
| POST | `/api/ai/veracity` | 内容真实性校验 | ❌ |

### 7.7 系统
| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/api/config` | 获取应用配置 | ❌ |
| PUT | `/api/config` | 更新应用配置 | ❌ |
| GET | `/api/health` | 健康检查 | ❌ |
| GET | `/api/test-env` | 测试环境变量加载 | ❌ |
| GET | `/api/test-veracity` | 测试 AI 真实性校验 | ❌ |

---

## 8. 部署需求

### 环境变量

**必需配置**：
| 变量 | 说明 |
|------|------|
| `DEEPSEEK_API_KEY` | DeepSeek API 密钥，用于 AI 摘要和质量评估 |
| `JWT_SECRET` | JWT 签名密钥，用于用户认证令牌签发 |

**数据源配置**：
| 变量 | 说明 |
|------|------|
| `YOUTUBE_API_KEY` | YouTube Data API v3 密钥 |
| `YOUTUBE_PROXY_URL` | YouTube API 代理地址（国内需要） |

**可选配置**：
| 变量 | 说明 |
|------|------|
| `SMTP_HOST` / `SMTP_PORT` / `SMTP_USER` / `SMTP_PASS` | 邮件服务配置 |
| `DB_PATH` | 数据库文件路径，默认为 `server/data/hot-monitor.db` |
| `PORT` | API 服务端口，默认为 3001 |
